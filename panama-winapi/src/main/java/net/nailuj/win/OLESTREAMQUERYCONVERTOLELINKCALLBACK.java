// Generated by jextract

package net.nailuj.win;

import java.lang.invoke.*;
import java.lang.foreign.*;
import java.nio.ByteOrder;
import java.util.*;
import java.util.function.*;
import java.util.stream.*;

import static java.lang.foreign.ValueLayout.*;
import static java.lang.foreign.MemoryLayout.PathElement.*;

/**
 * {@snippet lang=c :
 * typedef HRESULT (*OLESTREAMQUERYCONVERTOLELINKCALLBACK)(LPCLSID, LPOLESTR, LPOLESTR, LPOLESTR, LPOLESTR, ULONG, PVOID) __attribute__((stdcall))
 * }
 */
public class OLESTREAMQUERYCONVERTOLELINKCALLBACK {

    OLESTREAMQUERYCONVERTOLELINKCALLBACK() {
        // Should not be called directly
    }

    /**
     * The function pointer signature, expressed as a functional interface
     */
    public interface Function {
        int apply(MemorySegment pClsid, MemorySegment szClass, MemorySegment szTopicName, MemorySegment szItemName, MemorySegment szUNCName, int linkUpdatingOption, MemorySegment pvContext);
    }

    private static final FunctionDescriptor $DESC = FunctionDescriptor.of(
        winapi.C_LONG,
        winapi.C_POINTER,
        winapi.C_POINTER,
        winapi.C_POINTER,
        winapi.C_POINTER,
        winapi.C_POINTER,
        winapi.C_LONG,
        winapi.C_POINTER
    );

    /**
     * The descriptor of this function pointer
     */
    public static FunctionDescriptor descriptor() {
        return $DESC;
    }

    private static final MethodHandle UP$MH = winapi.upcallHandle(OLESTREAMQUERYCONVERTOLELINKCALLBACK.Function.class, "apply", $DESC);

    /**
     * Allocates a new upcall stub, whose implementation is defined by {@code fi}.
     * The lifetime of the returned segment is managed by {@code arena}
     */
    public static MemorySegment allocate(OLESTREAMQUERYCONVERTOLELINKCALLBACK.Function fi, Arena arena) {
        return Linker.nativeLinker().upcallStub(UP$MH.bindTo(fi), $DESC, arena);
    }

    private static final MethodHandle DOWN$MH = Linker.nativeLinker().downcallHandle($DESC);

    /**
     * Invoke the upcall stub {@code funcPtr}, with given parameters
     */
    public static int invoke(MemorySegment funcPtr,MemorySegment pClsid, MemorySegment szClass, MemorySegment szTopicName, MemorySegment szItemName, MemorySegment szUNCName, int linkUpdatingOption, MemorySegment pvContext) {
        try {
            return (int) DOWN$MH.invokeExact(funcPtr, pClsid, szClass, szTopicName, szItemName, szUNCName, linkUpdatingOption, pvContext);
        } catch (Throwable ex$) {
            throw new AssertionError("should not reach here", ex$);
        }
    }
}

